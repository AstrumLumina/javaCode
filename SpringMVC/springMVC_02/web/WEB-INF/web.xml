<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

<!--配置dispartchServlet ,这个是springmvc的核心: 请求分发器,前端控制器-->
    <servlet>
        <servlet-name>springmvc</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!--diapatcherServlet要绑定spring配置文件-->
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>classpath:springmvc-servlet.xml</param-value>
        </init-param>
        <!--配置启动级别:
        可能会有内置请求,服务已开启就要处理请求,所以这里将启动级别设为1,随服务器一起启动
        -->
        <load-on-startup>1</load-on-startup>

    </servlet>

    <!-- 再spirngmvc中, / :只匹配所有的请求,不会匹配jsp页面  即出现jsp后缀,直接返回该界面
                       /*  :匹配所有的请求,报错jsp页面     ,出现jsp页面,还会添加jsp后缀,无限嵌套下去
    -->
    <servlet-mapping>
        <servlet-name>springmvc</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>