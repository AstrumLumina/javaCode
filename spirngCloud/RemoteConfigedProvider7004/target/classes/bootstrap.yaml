spring:
  cloud:
    config:
      name: provider-config   #配置文件名字
      label: master #配置文件所在分支
      profile: test2 # 拿去该配置文件的具体种类
      #使用default时,只用 spirng.profiles.active 这一个信息,其他信息都拿不到,因此最好还是不用default,除非只有一个配置
      uri: http://localhost:6003

debug: true